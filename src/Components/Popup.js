import React from 'react'
import Button from 'react-bootstrap';

const Popup = () => {
    // handleClick = () => {
    //   this.props.toggle();
    // };
    return (
      <div>
        <Button variant="primary" size="lg" onClick={this.togglePop}>
          Click
        </Button>{' '}
      </div>
    );
}

export default Popup

